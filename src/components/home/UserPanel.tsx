// import React, { ReactElement, useContext } from 'react';
// import UserCard from "../UserCard";
// import { UserContext } from '../../context/userContext';
// import Project from '../../models/project';
// import { Flex } from '@chakra-ui/layout';

// function UserPanel(): ReactElement {
//   const { userProjects } = useContext(UserContext);
//   const projects = userProjects.map((project: Project, i: number) => {
//     <div className="userPanelProjects" key={i}>{project.title}</div>
//   });
//   return (
//     <Flex>
//       <Flex>
//         <UserCard />
//       </Flex>
//       <Flex>
//         {projects.length ? projects : 'no projects yet' }
//       </Flex>
//     </Flex>
//   )
// }

// export default UserPanel

export default {}