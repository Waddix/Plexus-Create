interface Profile {
  id: number,
  createdAt: Date,
  updatedAt: Date,
  user_id: number,
  username: number,
  email: string,
  image: string,
  title: string,
  bio: string,
  website: string
}

export default Profile;
