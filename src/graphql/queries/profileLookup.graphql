query profileLookup($id: Int!) {
  profileLookup(id: $id) {
    id
    name
    username
    title
    bio
    website
    image
    projects {
      id
      title
      description
      createdAt
      updatedAt
    }
  }
}
