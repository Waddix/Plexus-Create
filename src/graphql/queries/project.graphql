query Project($id: Int!) {
  project(id: $id) {
    id
    title
    image
    owner {
      username
      image
    }
    tags{
      name
    }
    ownerId
    description
    createdAt
    updatedAt
  }
}
