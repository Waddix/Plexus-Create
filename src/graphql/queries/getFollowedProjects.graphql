query getFollowedProjects($profileId: Int!) {
  getFollowedProjects(profileId: $profileId) {
    id
    title
    description
    ownerId
    owner {
      username
      image
    }
    tags{
      name
    }
    createdAt
    updatedAt
  }
}